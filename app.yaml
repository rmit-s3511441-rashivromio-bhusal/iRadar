# Google Cloud Platform file for specifying the App Engine
runtime: nodejs
env: flex

# Temporary setting to keep gcloud from uploading node_modules
skip_files:
 - ^node_modules$

env_variables:
  GCLOUD_STORAGE_BUCKET: iradar-specials
